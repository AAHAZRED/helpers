#!/usr/bin/bash

echo "
clone.sh             - clones a repos and config (arg: github URL).
connect_to_remote.sh - Connect local git repo to remote (arg: github URL).
correct_log.sh       - Change username and email address in git history (args: see script)
new_pat.sh           - Set new personal access token (PAT) in local git repo (no args).
new_repo.sh          - create new local repos and configs it (arg: user name).
rel_tag.sh           - creates and pushes new (release) tag. (args: tag, message).
"

